<UserControl x:Class="HotelManager.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HotelManager.ViewModels"
             Height="300" Width="400">
    <UserControl.DataContext>
        <vm:LoginViewModel />
    </UserControl.DataContext>

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Username" />
        <TextBox Width="200" Margin="5" Text="{Binding Username}" />
        <TextBlock Text="Password" />
        <PasswordBox Width="200" Margin="5" x:Name="pwdBox"/>
        <Button Content="Login" Width="200" Margin="5"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=pwdBox, Path=Password}" />
    </StackPanel>
</UserControl>
