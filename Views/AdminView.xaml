<Window x:Class="HotelManager.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelManager.Views"
        xmlns:vm="clr-namespace:HotelManager.ViewModels"
        mc:Ignorable="d"
        Title="AdminView" Height="450" Width="800">
    <Window.DataContext>
        <vm:AdminViewModel />
    </Window.DataContext>
    <Grid Margin="10,10,10,10">
        <!-- Grid for displaying accounts -->
        <DataGrid ItemsSource="{Binding Accounts}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedUser}"
                  Margin="0,0,0,40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" />
                <DataGridTextColumn Header="Full Name" Binding="{Binding Employee.FullName}" />
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Employee.Email}" />
                <DataGridTextColumn Header="Phone Number" Binding="{Binding Employee.PhoneNumber}" />

                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Command="{Binding DataContext.UpdateCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" />
                                <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" Margin="5,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Button to open new account form -->
        <Button Content="Add New Account" 
                Command="{Binding AddNewAccountCommand}" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Bottom" 
                Width="150" 
                Height="40" 
                Background="#4CAF50" 
                Foreground="White"
                FontWeight="Bold">
        </Button>
    </Grid>
</Window>
